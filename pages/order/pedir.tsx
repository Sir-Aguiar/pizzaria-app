import { NextPage } from "next";
import React from "react";
import styles from "../../styles/Pedir.module.css";

const Pedir: NextPage = () => {
  return <main className={`${styles.mainOrderContainer}`}></main>;
};

export default Pedir;
